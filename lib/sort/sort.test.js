const should = require('should');
const algo = require('../../index.js');

describe('Bubble Sort', () => {
	it.only('Should return sorted array by parameter', () => {
		const sort = new algo.Sort();
		console.log(sort);
	});
});
